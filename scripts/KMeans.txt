unsupervised clustering algoruthm, 
partitions the data into K clusters by minimizing distance to cluster centers
tests if uncertain prompts naturally form clusters in clip space

this one maybe not not very useful